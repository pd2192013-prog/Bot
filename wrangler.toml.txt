name = "mathbot-worker"
main = "worker.js"
compatibility_date = "2024-11-01"

# ---- D1 database (Google Sheets ka replacement) ----
# Pehle banao: wrangler d1 create mathbot-db
# Fir yahan neeche wali "database_id" ko us command ke output wali ID se replace karo.
[[d1_databases]]
binding = "DB"
database_name = "mathbot-db"
database_id = "REPLACE_WITH_YOUR_D1_DATABASE_ID"

# ---- KV namespace (PropertiesService / CacheService ka replacement) ----
# Pehle banao: wrangler kv namespace create BOT_KV
# Fir yahan neeche wali "id" ko us command ke output wali ID se replace karo.
[[kv_namespaces]]
binding = "BOT_KV"
id = "REPLACE_WITH_YOUR_KV_NAMESPACE_ID"

# ---- Auto-delete old chats: har 1 minute me check karta hai ----
# (Apps Script ke "everyMinutes(1)" trigger ka replacement)
[triggers]
crons = ["* * * * *"]
